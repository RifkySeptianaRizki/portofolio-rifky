--- src/components/Hero.tsx
+++ src/components/Hero.tsx
@@ -2,4 +2,4 @@
 
-import { useState, useEffect } from "react";
-import { motion } from "framer-motion";
-import { Plus, Grid3X3 } from "lucide-react";
+import { useState, useEffect, useRef } from "react";
+import { motion, AnimatePresence } from "framer-motion";
+import { Plus, Grid3X3, ArrowRight } from "lucide-react";
 
@@ -8,2 +8,3 @@
   const [time, setTime] = useState("");
+  const [isMenuOpen, setIsMenuOpen] = useState(false);
 
@@ -140,3 +141,6 @@
 
-            <button className="relative z-10 flex items-center gap-1.5 text-white text-[13px] font-bold px-4 py-2 hover:bg-white/10 transition-colors drop-shadow-sm">
+            <button 
+              onClick={() => setIsMenuOpen(!isMenuOpen)}
+              className="relative z-10 flex items-center gap-1.5 text-white text-[13px] font-bold px-4 py-2 hover:bg-white/10 transition-colors drop-shadow-sm"
+            >
               <Grid3X3 size={13} strokeWidth={2.5} /> Menu
@@ -146,2 +150,119 @@
 
+          {/* Full-screen Menu Overlay */}
+          <AnimatePresence>
+            {isMenuOpen && (
+              <motion.div
+                initial={{ opacity: 0, y: -20 }}
+                animate={{ opacity: 1, y: 0 }}
+                exit={{ opacity: 0, y: -20 }}
+                transition={{ duration: 0.4, ease: [0.16, 1, 0.3, 1] }}
+                className="absolute top-[80px] left-1/2 -translate-x-1/2 w-[90%] md:w-[500px] z-[100] rounded-[32px] overflow-hidden"
+                style={{
+                  background: "linear-gradient(180deg, rgba(25,25,25,0.85) 0%, rgba(15,15,15,0.95) 100%)",
+                  boxShadow: "0 30px 60px rgba(0,0,0,0.6), inset 0 0 0 1px rgba(255,255,255,0.1), inset 0 1px 0 rgba(255,255,255,0.15)",
+                  backdropFilter: "blur(30px)",
+                  WebkitBackdropFilter: "blur(30px)",
+                }}
+              >
+                {/* Top curved highlight reflection for the modal */}
+                <div 
+                  className="absolute pointer-events-none"
+                  style={{
+                    top: "1px", left: "10%", right: "10%", height: "20%",
+                    background: "linear-gradient(180deg, rgba(255,255,255,0.1) 0%, transparent 100%)",
+                    borderRadius: "0 0 50% 50% / 0 0 100% 100%",
+                  }}
+                />
+
+                <div className="p-8 pb-10 flex flex-col gap-6 relative z-10">
+                  {/* Header */}
+                  <div className="flex justify-between items-center border-b border-white/5 pb-4">
+                    <div className="flex items-center gap-2">
+                      <Grid3X3 size={14} className="text-white/60" />
+                      <span className="text-white text-sm font-semibold tracking-wide">Rifky</span>
+                    </div>
+                    <span className="text-white/40 text-xs font-medium">Available for work</span>
+                  </div>
+
+                  {/* Main Links */}
+                  <div className="flex flex-col gap-2">
+                    {[
+                      { name: "Home", color: "bg-white/10" },
+                      { name: "Projects", color: "bg-blue-500/20" },
+                      { name: "About", color: "bg-purple-500/20" }
+                    ].map((item, i) => (
+                      <a 
+                        key={i} 
+                        href="#" 
+                        className="group flex items-center justify-between py-4 border-b border-white/5 hover:border-white/10 transition-colors"
+                      >
+                        <span className="text-3xl md:text-4xl font-medium text-white/90 group-hover:text-white transition-colors">{item.name}</span>
+                        <div className={`w-14 h-14 md:w-16 md:h-16 rounded-2xl ${item.color} border border-white/10 flex items-center justify-center opacity-70 group-hover:opacity-100 group-hover:scale-105 transition-all duration-300`}>
+                          <ArrowRight className="text-white/50 -rotate-45 group-hover:text-white transition-colors" />
+                        </div>
+                      </a>
+                    ))}
+                  </div>
+
+                  {/* Social Media */}
+                  <div className="mt-4">
+                    <h3 className="text-white/30 text-xs font-semibold mb-3 uppercase tracking-wider">Social media</h3>
+                    <div className="flex flex-col gap-3">
+                      {['Behance', 'LinkedIn', 'Instagram'].map((social) => (
+                        <a key={social} href="#" className="text-white/60 hover:text-white text-sm font-medium transition-colors w-max">
+                          {social}
+                        </a>
+                      ))}
+                    </div>
+                  </div>
+                </div>
+              </motion.div>
+            )}
+          </AnimatePresence>
+
